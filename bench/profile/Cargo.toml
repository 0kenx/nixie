[package]
name = "bench-profile"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Profiling benchmarks for the Nixie SMT solver"
publish = false

[dependencies]
num-rational = { workspace = true, default-features = false, features = ["num-bigint"] }
nixie-core = { workspace = true, default-features = false, features = ["profiling"] }
nixie-proof = { workspace = true, features = ["profiling"] }
nixie-sat = { workspace = true, default-features = false, features = ["profiling"] }
nixie-solver = { workspace = true, default-features = false, features = ["profiling"] }
nixie-theories = { workspace = true, default-features = false, features = ["profiling"] }
rustc-hash = { workspace = true }

[dev-dependencies]
criterion.workspace = true

[[bench]]
name = "profile_benchmarks"
harness = false

[lints.clippy]
unwrap_used = "deny"
