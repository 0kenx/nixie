# Upstream v0.3.3 port — analysis and per-item disposition

Date: 2026-08-27. Branch `port/v0.3.3` (merged to `main`).
Upstream: `cool-japan/oxiz` tag `v0.3.3` (`e7c7bca`, "Availability of 0.3.3",
2026-08-26). This fork diverged from upstream at v0.3.1 (`88b7971`); the fork's
own v0.3.2 integration is documented in `INTEGRATION_NOTES.md`.

Method: read the full upstream `CHANGELOG.md` 0.3.3 section, enumerate every
item, and for each item either (a) port it (each its own commit, full gates),
(b) verify the fork already has it (run upstream's own test suite for it), or
(c) record why it is deliberately deferred. Nothing was skipped silently.

## Verification bar for the whole port

`cargo build --all-features` clean; `cargo nextest run --workspace
--all-features` 10 143/10 143 (one known-slow canary given a wider nextest
budget); `clippy -D warnings` clean; `fmt --check` clean; `cargo doc` clean
modulo 3 pre-existing oxiz-sat warnings present on main; z3 differential 160-161
solved / **0 soundness disagreements**; z3 parity 168 Correct / 0 Wrong /
2 Inconclusive over 170 benchmarks (standing level was 167/0/1 over 168; the
two additions are the port's own symbolic-RoundingMode cases).

## Per-item disposition

### Soundness fixes

| Upstream item | Disposition |
|---|---|
| `let` wrapper blinded pre-passes | **Already present** (fork commit `85f3a6f`, Z3-design parse-time resolution, predates v0.3.3's release) |
| Numeric-trichotomy-on-atom (A1) | **Already present** (fork's `ensure_numeric_equality_splits` + `add_arith_trichotomy_clause`; upstream's 24-test `arith_diseq_soundness.rs` passes on the fork unmodified) |
| `combine_eq` trail-polarity gate | **Behaviorally covered** (pigeonhole/distinct tests pass); fork's encode-time splitting is the primary mechanism |
| Lazy-mode pending-queue level tags | **Already present** (upstream's 4 `lazy_mode_*` tests pass) |
| `has_array_ops` set only from `Select` | **Already present** (fork's eager encode-time ext witnesses, `storecomm` comment) |
| Array-axiom budget exhaustion ⇒ "satisfied" | **Already present** (fork's honesty-gate design in `instantiate_array_axioms`) |
| Load-dependent verdict / case-split ceiling | **Already present, stronger** — fork removed the wall-clock gate entirely (2026-08-21, with the wisas/xs_8_13 load-dependence measurement) |
| `int_range_lp` (#46) | **Already present** (fork `8784060`, `lp_int_bounds` via `optimize_linexpr`, same soundness argument) |
| Store-chain depth 128 (latency) | **N/A** — fork's flat one-round RoW encoding subsumes it |
| SAT hanging unit after strengthening | **Ported** — propagation-head rewind at 4 sites (`remove_literal_and_rewatch`, vivify, inprocess, failed-literal probing); the LBD-clamp half was already present |
| Substitution fabricates 32-bit `concat` | **Ported** — `try_mk_bv_concat`, fail-safe call sites |
| `RecFunSolver::pop` arbitrary retraction | **Ported** — `app_trail` |
| **(found by the port)** clause-dedup memos survive `pop` | **Fixed** — `TrailOp::NumericEqSplitPairAdded` / `ArithConstAxiomPairAdded`; see below |
| **(found by the port)** write-map faithfulness under co-equal indices | **Fixed** — three layers guarded; see below |

### Features

| Upstream item | Disposition |
|---|---|
| `define-fun-rec` / `define-funs-rec` | **Ported** (parser `recfun.rs`, driver `context/recfun.rs` + `eval.rs`, 21 e2e tests) |
| `RoundingMode` first-class sort | **Ported** (14 tests; 3 pinned on a pre-existing FP-model gap — even concrete `(fp.add RNE c1 c2)` cannot produce a model value on main) |
| Gomory cuts reachable | **Ported** (root cut loop + `check_balanced`; fork files were identical to v0.3.2 modulo dash style) |
| BVE reachable + self-subsumption pass | **Partially N/A** — fork already has BVE wired (`enable_bve`) and its own cadical-style `subsume_round` with strengthening; the `>=`→`>` guard bug on the dead `Clause::self_subsuming_resolvent` primitive was ported; preset flips deferred to the benchmarking protocol (a measured knob decision, not a correctness one) |
| Model blocking (#40) | **Deferred** (self-contained follow-up; fork's `model_refutes_assertions` gate + Unknown path covers the honesty half) |
| `oxiz-core` Nelson-Oppen theory layer | **Deferred** (deliberately not wired into any solve path upstream either) |
| NLA relaxation engine | **Deferred** (largest single item, ~3k lines; own session) |
| Algebraic-number witnesses | **Deferred** (needs `NlDispatchResult` channel change + `model_fmt` root-obj printer; own session) |
| Lucky phase (#35) | **Already present** (fork implemented it 2026-07-28, before upstream's release) |
| `rhai` behind `scripting` | **Ported** |
| `nlsat` feature gate | **Deferred** (wasm-size motivated; feature plumbing + honesty gates, own session) |
| `oxiz-time` / wasm32 | **Deferred** (new crate + 47-file mechanical rewrite; own session) |
| Intern single-retention | **Deferred** (perf-memory item; needs the counting-allocator test) |

### Fixed / perf / config

| Upstream item | Disposition |
|---|---|
| Lexer infinite spin | **Ported** (+3 unit tests, +1 e2e test) |
| RegLan message accuracy | **Ported** |
| `IndexOfBounds` `-1` | **Ported** |
| `oxiz-core` no_std (`Reverse`) | **Ported** |
| 14 clippy-1.97 lints | **N/A** (tree already clippy-clean at our toolchain) |
| Denominator: `allow-wildcard-paths` | **Ported** |
| bench history-dir CWD-first | **Ported** |
| Deep-recursion tests intern directly | **Deferred** (test-only hygiene) |
| `solve_with_theory` trail clone (#42) | **Already present** (same in-place-slice design) |
| Hyper-binary BIG-path filter (#38) | **Ported** (the one code change; not the in-function filter, per upstream's own measurement) |
| `[profile.release-speed]` | **N/A** — upstream's bench inherited their `opt-level="z"` release; ours is already `opt-level=3` + fat LTO |
| `property-tests` default | **Ported** |
| nextest `terminate-after` | **Ported** (+ per-test override for the cxs-bp debug canary) |
| oxiarc 0.4.0→0.4.1 | **Not ported** (version bump only; no behavior) |

## The two bugs the port itself found

### 1. Write-map faithfulness under co-equal indices (false `unsat`)

Upstream's `store_commutativity_without_distinctness_is_sat` control failed on
the fork: `(not (= (store (store a i x) j y) (store (store a j y) i x)))` is SAT
(take `i = j, x ≠ y`) and the fork answered **`unsat`**. Root pattern: a
store-chain write *map* `{i₁→v₁, …, iₖ→vₖ}` is a faithful model of its chain
only under pairwise-distinct indices — with co-equal symbolic indices the two
orderings keep different survivors at the shared index. Three layers shared the
blindness; all fixed:

- `check_array::store_chains_concretely_equal` (the syntactic storecomm fast
  path — the layer that produced the false verdict): "concretely equal" now
  additionally requires every chain's own index terms pairwise *provably*
  distinct (distinct literals); otherwise `None` → CDCL(T).
- `finite_disjunction_extensionality`: the flat value-disjunction lemma is
  emitted guarded by each chain's pairwise index distinctness; `is_complete`
  only when unguarded, so the fresh-witness clause is still minted for
  symbolic-index pairs.
- `build_read_over_write` flat SAME-implications (direct + aliased):
  order-aware guards (`row_same_guard`) — an inner write is visible only where
  the read index misses every outer write it could co-equal.

### 2. Clause-dedup memos survive `pop` (false `sat`, pre-existing on main)

The `define-fun-rec` port's `get_consequences` e2e test failed; reduction
removed `define-fun-rec` entirely and produced a 5-line QF_LIA repro that fails
on **pristine main**:

```text
(assert (= p (= x 5))) (assert (not p)) (push 1) (check-sat) (pop 1)
(assert (= x 5)) (check-sat)   ; must be unsat; main answers sat
```

Investigation path (recorded so it is not repeated): an initial "stale watch
lists in `oxiz_sat::Solver::pop`" theory was **disproved** — a standalone
`oxiz-sat` replay of the same clause/unit sequence answers correctly, and the
SAT cores of the working and failing checks turned out isomorphic. Clause-level
backtrace tracing (`OXIZ_SAT_TRACE=2` + `std::backtrace`) attributed the
missing constraints to the lazy check-time passes, whose dedup memos state the
false assumption in their own comments ("persists in the SAT core … never
removes base clauses" / "whose clauses survived (no retracting `pop`)"): a
clause emitted during a check that runs **inside a push scope** is retracted by
`sat.pop()` with the scope, the memo key survives, and the clause is never
re-emitted. Two instances fixed (`numeric_eq_split_pairs`,
`arith_const_axiom_pairs`); the class swept (`care_split_pairs` and
`arith_defined_terms` were already journalled). One process note: an
intermediate "fix doesn't work" observation was a **stale binary** — a
disk-full build failure left the previous executable in place; the memo fix
alone repairs every repro once the binary is genuinely rebuilt.

## Notable adaptations vs upstream

- No `RoundingMode`-position rejection inside `parser/recfun.rs` until the
  RoundingMode port landed (it did, in the same series).
- `Context::check_sat` split into `check_sat_core` + recfun dispatch; the
  fork's logic-contract validation and array honesty gate stay inside the core
  so every fuel round runs through them.
- The recfun driver's `check_round` drops upstream's `rounding_mode_used`
  distinctness call (the fork asserts distinctness in `check_sat_core`, which
  the plain rounds go through).
- `Context::parse_sort_name` needed its own `"RoundingMode"` arm — the first
  wiring attempt silently declared the constant at a *different* SortId than
  the parser's, leaving the closure axiom un-asserted (caught by the
  `@uc_RoundingMode_0` model output).

## Postscript (2026-08-28): the deferred list is closed

Every item above is now landed, each as its own commit on `main`:

1. **Symbolic-rounding-mode FP solving** (the pinned gap's actual root:
   upstream's `IteBranch` fix in the FP model finder — the finder evaluated
   BOTH `ite` branches, so the parser's five-way symbolic-mode expansion made
   every arm but the taken one unevaluable).  All 3 pinned tests un-pinned,
   14/14; `fp_symbolic_rm_sat` parity benchmark now matches z3: **169/170
   Correct, 1 Inconclusive** — the best parity the tree has recorded.
2. **Model blocking (#40)** — and it fixed a real fork hazard: the fork's
   inline blocker excluded decision paths but never downgraded a later `Unsat`
   over its own restriction clauses (a false `unsat` reachable whenever the
   refutation gate fired on the evaluator's width limit).  Verdict lattice
   `Unknown -> {Sat, Unknown}` restored at all three `Unsat` sites.
3. **`oxiz-time`** — 126 `std::time` references / 72 files across the ten
   library crates; feature wiring + `IS_FROZEN` guards per crate.
4. **Intern single-retention** (`HashTable<TermId>`, counting-allocator slope
   test), **`oxiz-core` theories layer** (Theory impls + Nelson-Oppen
   fixpoint; upstream file verbatim), **`nlsat` feature gate** (honest cfg
   splits; the fork's whole NL dispatch routes through the gated core, so OFF
   declines QF_NIA witnesses — upstream's relaxation engine, landed next,
   recovers them).
5. **NLA relaxation engine** (~4.1k lines incl. tests; `x,y>=3 AND xy<=8`
   unknown->unsat, differential par2 best-yet), **algebraic-number witnesses**
   (`x*x = 2.0` -> `sat` with `(root-obj (+ (^ x 2) (- 2)) 1)`, byte-identical
   to z3 4.15.4; the five root-isolation fixes ride along).
6. **Deep-recursion test hygiene**; **BVE preset decision — NOT flipped,
   deliberately**: upstream enables BVE in `Industrial`/`CaDiCaL`/`thorough`,
   but a preset flip is a heuristic change under this repo's rules
   (`docs/BENCHMARKING.md`: matched null, >=10 seeds, pre-registration), and
   no such measurement exists here. The upstream data point is recorded;
   flipping without the protocol is exactly what the protocol exists to
   prevent. The `enable_bve` knob itself has been reachable here since the
   fork's own earlier work.

## Second-pass scan (2026-08-28): the code-level diff audit

A file-and-hunk absorption audit of the whole `v0.3.2..v0.3.3` diff against
this tree (every changed file's upstream-added lines tested for presence
here, then each non-absorbed hunk read on both sides). Landed from it
(`oxiz-scan` branch):

- **Algebraic-channel entry-clear** (`dispatch_nl_solver`): a repeated
  `check` on an unchanged stack never passes through
  `invalidate_results`, so a stale `root-obj` from an earlier check could
  outlive the verdict that produced it. Cleared at the entry of the only
  procedure that populates it (upstream's rule: populated means populated by
  the procedure that just answered). Pinned by a repeated-check e2e test.
- **NL-witness publication gate** (`install_nl_model` + new
  `nl_eval::refuted_under`): the cell-decomposition dispatcher's rational
  witness is re-evaluated against every assertion in exact `BigRational`
  before installation; a witness some assertion definitely evaluates FALSE
  under is a proof it is not a model and publishes nothing. Fork-adapted to
  the strict-negative dual of upstream's `holds_under` (abstention on opaque
  array leaves is not refutation — the fork's model searches verify their
  goals their own way; upstream's dispatcher *is* the verification).
- **Trail-first polarity reads** in the datatype model pipeline
  (`decided_dt_equalities`, `decided_constructor`): decided equality/tester
  polarity now comes from the SAT core's final assignment with the model
  entry as fallback, making the repair read commitments rather than
  trajectory-dependent recording artifacts.
- **`theory_trail_suffix_delivery_regression.rs`** (upstream issue #42):
  the fork already had the O(trail²) fix; the delivery-contract test now
  pins it (2 tests).
- **wasm `feature_registry` honesty** (upstream v0.3.3): the old ~645 KB /
  ~35-45% claims were never true — `proof`/`interpolation`/`spacer`/
  `ml_branching`/`wasm_bench` are empty feature lists gating nothing.
  `nlsat` is now a real optional edge (the one measured lever: ~194 KB raw /
  ~68 KB gzip), exposed via `has_nlsat()`, excluded by `minimal`, included
  by `full`/default.
- **`a_rational_nl_witness_survives_reverification`** e2e pin.

**Found, root-caused eight layers deep, deliberately NOT landed** — the
biggest find of the scan, deferred as a unit to the next session:

- **`model_violates_negated_equality`** (the soundness gate's missing half:
  a numeric `Eq` the core assigned false whose model collides both sides is
  a definite refutation) — ported and verified, but it *honestly reports*
  inconsistencies this fork's pipelines currently produce: the
  array-extensionality witness atoms and datatype-axiom field equalities
  are committed false by the core without the LP learning the strict
  separation. Upstream's companion fix (their A1) emits the trichotomy
  clause `(a=b) ∨ (a<b) ∨ (a>b)` from the *encoder's `Eq` arm* so every
  numeric equality — including theory-axiom atoms — carries it. Porting A1
  fixed every one of the gate's spurious `Unknown`s (array, qfidl, dt all
  went green) but perturbed CDCL trajectories enough to expose **three
  pre-existing fragilities**, each a real bug on its own:
  1. the datatype repair can read a stale `Some(true)` polarity for the
     `(= p q)` atom family and decline to separate two constants the search
     proved distinct (wrong *witness*, verdict correct; the
     `get_model_disequal_records_get_distinct_witnesses` pin);
  2. the simplex snap delta-propagation hit a `debug_assert` mismatch
     (`got 4, want 1`: a stale pre-update assignment, wrong in *release*
     silently — `lia_integration::test_div_mod_abs_differential_against_
     brute_force`);
  3. the recfun certified-model search declined on the new trajectory
     (`symbolic_argument_solves_for_the_variable`: unknown vs sat).
  Landing A1+gate therefore requires fixing those three first (a var-level
  trail dump of the recognisor axiom atoms — ids 4/13/30/55 in the
  40-entry build — is the next session's opening move). The gate function
  ships `#[allow(dead_code)]` with the trichotomy block ready in the study.
  **None of this is main's regression**: on main's trajectories the three
  bugs sleep; the scan's port is what woke them.

**Verified already-present / deliberate divergences (not ported):**
`self_subsumption` (fork's own pass in `bve.rs` + the equal-length `>` fix);
`lucky.rs` (fork = CaDiCaL trail port vs upstream's scratch-buffer verified
design — both sound); `int_case_split`/`int_range_lp` (fork's own 690-line
engine; Wisa fixture and store-commutativity verified `unsat` empirically,
matching z3); `proof_log`/`declare_datatype` (inline in fork's `context.rs`);
`REFINEMENT_TIME_CEILING_MS` (fork removed wall-clock ceilings on
principle); solver-level `enable_bve` (fork's `OXIZ_FREEZE_COLLAPSE` knob
already runs SAT BVE by default in the CDCL(T) path, behind its own
measured study); BVE preset flips (benchmarking-protocol-gated); `let` in
the model evaluator (fork's parser resolves `let` at parse time — A2 — so
`TermKind::Let` never reaches it); cuts.rs frontier tests, preprocessing
issue36 tests (test-only, different implementations); version bumps,
dash-style, oxiz-time rewrites, deep-stack test hygiene (landed or N/A).

## Third session (2026-08-28, later): the three exposed bugs dug to the bottom

All three bugs the A1 trajectory change woke were root-caused with
entry-invariant traps and backtrace pinning, and the first two are FIXED and
landed (`5d5ba84`):

1. **Simplex pop/assignment staleness** - three cooperating holes: (a)
   `intern_row_cached` computes a new slack's value only when
   `assignment_current` is up, so a slack interned while the flag was down
   keeps its `zero()` default forever; (b) `pop` restores the pre-mutation
   assignment snapshot but only marked staleness when the vector GREW - at
   unchanged length the restored values do not satisfy the restored tableau,
   flag still up, and shape (a) can launder a wrong value into the scope
   snapshot as if consistent; (c) `pivot` and `on_nonbasic_bound_change`
   consumed the vector without honoring the flag, so
   `propagate_bounds`/`tighten_bounds` pivoted on stale values (release builds
   use them silently). Fix: pop marks stale on ANY mutated scope, both
   consumers re-derive when the flag is down. EUF's own pop/conflict
   machinery was verified correct by a micro-test (merge chain vs live diseq
   conflicts).
2. **Wrong datatype witness** - `decided_dt_equalities` read SAT-true `Eq`
   polarity as "equal", but recognisor/congruence axioms' consequents are
   free Booleans; one set true under an unproven premise chained
   `p = C(...) = C'(...) = q` and the repair declined on a pair it had to
   separate. Fix: the two directions read different authorities - disequality
   from the SAT commitment, equality from EUF congruence classes only.
   The pinned test is un-ignored and green.
3. **Gate re-derived interface equalities from cross-theory artifacts** - a
   purification definition `(= (f x) $p)` splits `f(x)`'s value (EUF side)
   from `$p`'s (arithmetic side); reading both back and comparing reports the
   mismatch the definition papers over, and every candidate of the
   `sum(k)=6` refinement loop hit it. Fix: conjunct-level assertion checking
   where an `Eq`/`Not(Eq)` conjunct is decided by its SAT-committed polarity
   (a contradicting commitment still refutes - the gate keeps its teeth).

**Still deferred, dependency now precise**: the A1 trichotomy + the gate's
negated-equality half. The trichotomy is what makes committed-false
equalities reach the LP as strict bounds (so the gate's second half stops
reporting collisions the fork repairs late), but its trajectory exposes a
FOURTH latent gap: **ite-guard conditional equalities never reach the
tableau** - the gate honestly reports `(=> (<= x 0) (= 0 ite_x))` evaluating
false (guard true in the LP, ite value != 0) on the recfun `sum(k)=6` goal,
and the blocking loop cannot recover within budget (starves to unknown).
Both code blocks are ready in this history; landing them is one fix away:
relay the ite-guard conditional eq to arith when its guard commits.

## Fourth session (2026-08-28, evening): the A1 landing attempt, measured three ways

With the three roots fixed, the A1 trichotomy + gate second half were re-run:
all suites green (solver+theories+sat 4779/4779, recfun `sum(k)=6` back to
`sat k=3` — the third session's "fourth gap" was actually covered by the
conjunct-settled gate fix).  The blocker is now MEASURED, not hypothesised:

- **Blanket arm-emission** (upstream's shape): differential canary 163 → 150
  solved, par2 2243 → 2494 (15 regressions / 6 gains, 0 disagreements either
  way).  Regressions concentrate in QF_ANIA / QF_AUFLIA storecomm/swap/floppy
  and QF_UFLIA Hash/Wisa — the array-axiom families where the added clauses
  re-time the search backbone.
- **Phase-scoped emission** (emit only for theory-minted atoms; assertion
  atoms keep the walk's exact clause order): 151 solved.  Barely better — the
  axiom atoms ARE the search on exactly those families.
- **Lazy collision repair** (zero clauses until the gate observes a
  committed-false-eq collision; then emit that pair's trichotomy and
  block+rebase — the CDCL way): 3 extensionality/datatype tests still fail.
  The probe: the clause IS added (6→7 clauses), the next candidate commits
  `eq=false` with the strict arm true, and the model values STILL collide —
  the strict bound's relay vs the value source (DL/simplex routing, or the
  rebase/reset assignment lifecycle) does not close the loop.  That residual
  is the next session's precise opening item.

Per `docs/BENCHMARKING.md`, a −10..−13 net on the canary is not landable
without the matched-null/multi-seed protocol, and the lazy variant is not yet
correct.  Nothing from this session landed on main (which stays at the three
root-cause fixes, 10402/10402, differential 162-163/0, parity 169/0/1).  The
lazy-variant implementation is preserved on branch `fix/ite-guard-relay`
(worktree `oxiz-ite` removed after documentation) — it contains
`refuted_negated_equality` returning the colliding pair, the
`emit_collision_trichotomy` repair hook at the `model_refutes_assertions`
call site, and the tracing probes that established the clause-is-added /
values-still-collide fact.

## Fifth session (2026-08-28, night): the lazy repair root-caused and the landable half landed

The lazy variant's residual ("clause added, strict arm committed, values still
collide") was rooted precisely: **the colliding terms were Select terms with
zero arithmetic presence** — the collision's *value source* (model entries,
built by the array/defaulting path) and the trichotomy's *constraint target*
(arith terms) are disjoint worlds. A strict bound can never separate what the
model builder does not consult. The correct repair is at the model level.

**Landed (`12fe54e`, merged as `ddcc4d7`)**:
`separate_committed_disequalities` in the model builder — for every
committed-false equality whose sides render equal, bump one side (Int/Real)
when nothing pins it; rebuild constructor-valued entries containing the
bumped term (plus selector entries over them, matched by old field value);
re-verify every committed-true atom mentioning a bumped side and revert the
bump wholesale if any would flip to definitely-false.  Deliberately scoped
OUT: the datatype value-graph (selector applications and datatype-sorted
terms — their recognisor tautologies put every side under committed-true
atoms, and bumping without re-deriving the graph just reverts; measured in
traces).  Result: the array extensionality witness shape is fixed (15/15
array_axiom_instantiation with the gate's collision half enabled), zero
clauses added anywhere, differential canary at baseline (163/0, par2 2245 in
the 2243–2252 band), parity 169/0/1, workspace 10402/10402.

**Still gated (the enablement checklist for the collision half)**:
1. datatype selector value-graph re-derivation after a bump (essentially:
   re-run the reconstruction with one field forced — the machinery lives in
   `extract_datatype_model`/`open_dt_value`; the failing pins are
   `get_model_disequal_records_get_distinct_witnesses` on the pure-equality
   path and `test_dt_differential_against_brute_force_oracle`);
2. storecomm-style pinned-side pairs (`store_commutativity_without_
   distinctness_is_sat`): the candidate needs `i = j, x != y`, the blocking
   loop must converge to it — a search matter (the full-assignment
   projection blocks one point at a time over a space with free array vars).
Both helpers (`refuted_negated_equality`, `refuted_by_assertion_only`) ship
ready and documented-off in `model_eval.rs`.

## Sixth session (2026-08-29): the collision half is LIVE

Both enablement-checklist items resolved into a working landing (`5e9c641`):

- **Selector re-derivation** (`rederive_selector_entries`): `sel(C(a…)) = a_i`,
  folded syntactically over literal constructor applications and re-derived
  from model entries otherwise, to a fixpoint.  A separated datatype value now
  propagates to the whole derived selector graph — the missing piece that left
  recognisor/congruence selector terms colliding after the constructors
  themselves were separated.
- **Field hints + injectivity** (`dt_field_hints`, `dt_injectivity_pairs`): a
  committed-false selector equality names the FIELD to separate (the repair
  used to pick the first bumpable field; each blocked round re-picked a
  different one and never converged); same-constructor selector
  disequalities derive arg-level disequalities (constructor injectivity) so
  the repair fires even without a committed-false top-level atom.  Multi-field
  bumps are cumulative.
- **Proxy-aware leaf resolution**: the gate's evaluator prefers a concrete
  model entry over the tableau for the solver's own variables
  (`__oxiz_*`/`$p*`) — repairs publish separated constants there while the
  tableau holds unconstrained defaults.  USER variables stay tableau-first
  (model-first flipped `array_unique` on parity).
- **Scope, each exclusion root-caused**: plain numeric sides only.  Arrays are
  out (read value-graph + store-store congruence — store-commutativity
  candidates carry `i=j, x=y` and the core re-proposes the same vacuous shape
  until the block budget dies); `dt.size!` measures and `DtSelector` sides are
  out (derived from the reconstruction; enum-heavy goals produce thousands of
  same-constructor pairs nothing retires — measured 3 096 on `dt_09`).
  `enable_collision_gate` config; the model counter and `minimal()` opt out
  (enumeration reads every Unknown as end-of-enumeration).

**The A1 arc's final state**: upstream's trichotomy clause-emission itself
stays unlanded (its measured trajectory cost: −13 blanket / −10 scoped on the
canary; the lazy clause variant's residual was root-caused to the disjoint
value-source vs constraint-target split, resolved instead by the model-side
repairs).  The gate it was meant to enable is live, with its teeth: an
assertion-level false, an `Unrepresentable`, or an in-scope collision each
refute a candidate `Sat`.  Gates: 10402/10402, parity **169/0/1 fully
restored** (dt_05/dt_09 `sat` again), differential 162/0 par2 2258 (baseline
band), zero clauses added anywhere.

Remaining tracked: the array value-graph re-derivation + store-store
congruence propagation (retires the array scope), the dt.size/DtSelector
re-derivation engine (retires the datatype scope).

## Seventh session (2026-08-29): the array scope retired at its roots

The store-commutativity blocker dissolved into three z3-verified fixes
(`c91a148`, merged):

1. **Store-store congruence** (the wrong-model `sat`): main shipped `sat`
   with `i=j=x=y=0` for the store-permutation disequality — a model under
   which the chains are identical and the assertion false.  The congruence
   axiom family (`(= b1 b2) ∧ (= i1 i2) ∧ (= v1 v2) → (= S1 S2)`) now
   instantiates for same-base pairs and pairs the formula already equates;
   premise numeric pairs emit their trichotomy (lazy A1 for lemma-borne
   atoms).  Verified: `sat` with `i=j=0, x≠y`.
2. **Alias poisoning** (a real wrong-lemma bug the new axioms exposed): the
   array walk's saturation pass re-walks asserted lemmas, and its `Eq` arm
   recorded LEMMA ANTECEDENT equalities as unconditional aliases — the
   congruence premise `(= a (store a i x))`, true in no model — from which
   the upward/row builders derived `x = select(a, i)` and kin.  z3-verified
   six of fifty-two asserted instances INVALID; the chain forced `x=y` and
   answered `unsat` for a `sat` goal.  Aliases now record from input
   equalities only.
3. **The pure-equality path claiming numeric goals**: `(not (= x y))` in
   plain QF_LIA rode the chordal-SAT path (no arithmetic at all) —
   pre-gate main answered `sat` with `x=y=0` (wrong model); collision-gate
   main answered `unknown`.  Plain-arithmetic goals now opt out to the
   general path where the assertion-walk trichotomy splits the disequality
   (`sat`, `x=0 y=-1`).  UF/array-bearing goals keep the fast path (routing
   them out was measured at several differential timeouts).

This also retires the sixth session's "storecomm-style pinned-side pairs"
checklist item — the pinned pairs were never a search-convergence matter;
the candidates were vacuous because nothing propagated chain equality.

Gates: workspace 10402/10402 (pre-merge, worktree), clippy clean, fmt,
z3 parity **169/0/1** (verdict-based, load-robust).  **Measurement debt**:
the differential canary could not produce a clean A/B today (machine at
near-100% load, wall-clock timeouts noise-dominated; `pete_cxs_bp` —
nominal ~230 s — times out on both trees identically).  The A/B vs
`65c26eb` must run on a quiet day before further array-layer work; the
unfiltered O(n²) axiom form measured −12 solves and is NOT what landed.

## Eighth session (2026-08-29, later): dt.size re-derivation landed

`rederive_size_measures` (`674e18d`, merged): every `dt.size!<id>` measure
variable's model entry is computed from the term it measures —
`size(C(fields)) = 1 + Σ size(datatype fields)` — as a memoised worklist over
the reconstructed constructor values.  The measure is only ever *constrained*
by the axioms (non-negative, `outer > inner`, congruence), never defined, so
`(get-value)` printed tableau defaults (0) for every size; now it prints the
real size.

The collision gate's size-side exclusion stays, with the convergence wall now
precisely measured: distinct trees can genuinely have equal sizes (two
leaves), the core commits `size_a ≠ size_b` as a free Boolean no axiom
forces, and on a structurally forced goal the blocking loop burns its budget
(dt_05: 38 rounds).  Enabling size sides needs a size-splitting repair — bump
one side's derived size while re-establishing the `outer > inner` ordering —
which replaces the previous "dt.size/DtSelector re-derivation engine" tracker
item with a sharper one.

The seventh session's measurement debt (canary A/B vs `65c26eb`) remains
pending — the machine ran at load 38–118 all day; the workspace suite passed
at `-j8` under it, and parity (verdict-based, load-robust) held 169/0/1
throughout.

## Ninth session (2026-08-29, evening): the size-splitting repair — the wall closed

`7641e47` (merged): a committed-false size equality whose sides derived EQUAL
canonical sizes is now split at the model level — one side's measured term
gets a `+1` size override and every other size re-derives through it, which
re-establishes `outer > inner` by construction (`1 + Σ children > each
child`).  Soundness guard: a side is bumped only when its measured term's
reconstructed value is unshared (a shared value could mean a committed-true
term equality, whose congruence would require the sizes to stay equal).
`side_ok` admits `dt.size` vars with authoritative derivations.

dt_05 — the 38-block-round convergence wall — now answers `sat` directly, and
dt_09 stays `sat`.  The collision gate's datatype scope is now sizes-covered;
what remains excluded is the enum-heavy `DtSelector` shapes (selector pairs
under free-Boolean congruence, thousands per goal) and the array read-graph.

Gates: workspace 10402/10402 (at `-j6` under load 32–113), clippy clean, fmt,
z3 parity 169/0/1 (verdict-based, load-robust).  The seventh session's canary
A/B debt remains pending on a quiet machine.

## Tenth session (2026-08-29, night): the canary A/B debt retired — the trade measured right

The machine finally went quiet (load ~5 after a reboot) and the pending A/B ran
clean, both binaries back-to-back:

- **Bare gate**: baseline `65c26eb` 164 solved / par2 2228.63; treatment
  `6cfbe9d` 159 / 2327.89.  Five flips, all decomposed and none a soundness
  loss: `floppy2` ×3 sat→unknown at ~3 s (the alias fix's honest lemma mix
  runs the search past the conflict budget — `resource_exhausted`, precision
  not soundness; the congruence axioms do not even apply: 1 store in the
  file), and `storecomm` ×2 timeouts at the 10 s cap that both solve `sat`
  at 26–60 s (the axiom saturation on 100-store chains; a cap sweep to 8
  pairs showed the cap is not the lever and was discarded).
- **Model validation** (`--validate-models`, the trusted view): baseline ships
  FIVE invalid-model sats — three in `QF_AUFLIA/swap` (the exact
  store-permutation wrong-model class the congruence family targets) plus
  `DLX1C0`/`aodv5` (UCLID-pred, an unrelated family).  The treatment ships
  TWO — `DLX1C0`/`aodv5` only.  **The swap class is eliminated.**

Verdict: −5 ten-second-cap solves bought the retirement of three
wrong-model `sat`s and the two z3-verified wrong-answer bugs of the seventh
session.  A dead-weight trim (skipping premise trichotomies for
provably-distinct indices) was measured as no-canary-change and dropped —
nothing lands without a measured reason.

Remaining wrong-model families after this arc: `QF_UFIDL/UCLID-pred`
(`DLX1C0`, `aodv5`) — UF+IDL recognisor goals, not array/store shapes.

Two fork-divergence pins remain recorded at their sites (both honest
`Unknown`s, never wrong verdicts): the engine's neg*neg witness test and the
multivariate `x*y=6 AND x+y=5` OFF-build goal — engine code is verbatim, the
divergence is in a LIA sub-solve dependency; tracked for a dedicated dig.

## Postscript: corpus-destruction incident and restoration

**What happened.** During the port, the worktree needed the untracked corpora;
three links (`satcomp2024`, `satcomp2025`, `satlib` → the primary tree) were
accidentally committed by a `git add -A` (a `.gitignore` entry with a trailing
slash — `/satcomp2024/` — ignores the *directory* but not a *symlink* of the
same name). Landing that commit on `main` made git check the symlink out over
the primary tree's real directory: **git clobbers ignored paths at checkout**,
so the real `satcomp2024/`, `satcomp2025/` and `satlib/` directories (the
curated GBDB-layout corpora) were destroyed. `smt-lib/` survived (its link was
one level down, inside the directory).

**Restored.** Every test-consumed corpus file (4 files, `oxiz-sat`'s
watch-position/elimination/shrink/ELS regressions) was re-fetched from the
Global Benchmark Database (`https://benchmark-database.de/file/<md5>?context=cnf`,
xz-compressed) and is **byte-identical** to the lost copy (md5 checked against
a value recorded earlier in the session). `cargo nextest -p oxiz-sat` is
870/870 again. `satcomp2025/main_easy_mid/6s167-opt.cnf` (one benchstore cell
input) was not re-fetched; the result store never re-runs existing cells.

**Not restorable from record.** The wider curated corpora (the 45/60-file
standing sets named by short name in the studies) were not recorded with their
sha/md5 prefixes anywhere in-tree. Rebuild them with the same one-line recipe
per file (`curl -sL "https://benchmark-database.de/file/<md5>?context=cnf" |
xz -d > satcomp2024/bench/<md5>-<name>.cnf`); the standing-table file names are
in `docs/studies/2026-08-satcomp-standing-gap.md`.

**Lesson (now also implied by the cleanup rules).** (1) Never let a symlink
into an index path where the target name is a gitignored *directory* — the
trailing-slash ignore does not cover it, and checkout will clobber the real
data. (2) `git add -A` in a worktree that had to synthesize corpus links is
how it got in; add paths explicitly. (3) The one reason this was recoverable
at all: the corpora are external and content-addressed (GBDB md5s in the file
names).
