# Tails campaign tranche 1: the four chaos files at ≥10 seeds (2026-09-12)

Priority-3 from the round-8 handoff: frb65 (30× seed spread), mdp (17×),
mp1-Nb7T42 (22×) could not support claims at 5 seeds.  Seeds 5-9 were run
on the standing baseline binary (`precompile/8082e335`, default config,
60 s cap, model-checked SAT cells — same protocol as the re-baseline;
cells filed under `precompile/8082e335/benchmark/runs/sc24f/`), joining
the seeds 0-4 re-baseline cells for the ≥10-seed picture.  mp1-klieber
was included as the fourth member of the class.

| file | solved /10 | conflicts geomean (solved cells) | spread |
|---|---|---|---|
| frb65-12-2 | **10/10** | 436,192 | 36.6× |
| mp1-Nb7T42 | 9/10 (s3 TO) | 44,413 | **174×** |
| mdp-28-14 | 3/10 | 359,743 | 16.9× |
| mp1-klieber-t12 | 3/10 | 104,497 | 5.9× |

## Verdicts

- **frb65-12-2 and mp1-Nb7T42 are converts**: every (resp. 9 of 10) seed
  solves within the cap.  They are not "lucky cells" — but with 36-174×
  within-class spread, *any A/B claim touching them must report the ≥10-
  seed geomean*, never a 5-seed median (the round-6/7 lesson, now with
  the cells to enforce it).
- **mdp-28-14 and mp1-klieber stay endurance files** at the 60 s cap
  (3/10 each; the solving seeds need 0.6-1.1 M conflicts).  Cells exist
  for cross-checking but they cannot arbitrate a <2× effect at this cap;
  widen the cap or accept them as 0-class rows.

Remaining priority-3 item (not done in this tranche): the re-screen of
the single-seed-screened policy landings (Gent saved-position) at 5 seeds
— two more 270-cell campaigns on the same protocol.

Tiered-arm note: the `NIXIE_TIERED` screen (same day) shows mp1-Nb7T42
4/5 → 1/5 and mdp 2/5 → 1/5 — both confirmed members of the
restart-hungry loser class (see the tiered-port study).
