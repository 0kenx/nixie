<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1600" height="382" onload="init(evt)" viewBox="0 0 1600 382" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = false;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="382" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">j3037: structural definition elimination (prototype 54dd3ad)</text><text id="subtitle" x="50.0000%" y="48.00">CPU 15; 21,328 samples; qualified LBR capture; sampled-prefix attribution</text><text id="details" fill="rgb(0,0,0)" x="10" y="365.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1590" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1590" y="365.00"> </text><svg id="frames" x="10" width="1580" total_samples="223366076481"><g><title>core::slice::sort::stable::driftsort_main (1,413,822,175 sampled cycles, 0.63%)</title><rect x="4.7825%" y="189" width="0.6330%" height="15" fill="rgb(246,15,49)" fg:x="10682476148" fg:w="1413822175"/><text x="4.9700%" y="199.50"></text></g><g><title>core::slice::sort::stable::quicksort::quicksort (1,350,984,691 sampled cycles, 0.60%)</title><rect x="4.8106%" y="173" width="0.6048%" height="15" fill="rgb(222,226,6)" fg:x="10745313632" fg:w="1350984691"/><text x="4.9981%" y="183.50"></text></g><g><title>core::slice::sort::stable::quicksort::quicksort (942,544,750 sampled cycles, 0.42%)</title><rect x="4.9935%" y="157" width="0.4220%" height="15" fill="rgb(222,226,6)" fg:x="11153753573" fg:w="942544750"/><text x="5.1810%" y="167.50"></text></g><g><title>core::slice::sort::stable::quicksort::quicksort (544,582,474 sampled cycles, 0.24%)</title><rect x="5.1717%" y="141" width="0.2438%" height="15" fill="rgb(222,226,6)" fg:x="11551715849" fg:w="544582474"/><text x="5.3592%" y="151.50"></text></g><g><title>nixie_sat::solver::Solver::mark_subsume_lit (450,347,571 sampled cycles, 0.20%)</title><rect x="5.4858%" y="189" width="0.2016%" height="15" fill="rgb(240,156,52)" fg:x="12253390009" fg:w="450347571"/><text x="5.6733%" y="199.50"></text></g><g><title>core::slice::sort::unstable::quicksort::quicksort (565,536,018 sampled cycles, 0.25%)</title><rect x="8.4021%" y="173" width="0.2532%" height="15" fill="rgb(213,12,20)" fg:x="18767528545" fg:w="565536018"/><text x="8.5896%" y="183.50"></text></g><g><title>nixie_sat::solver::conflict::&lt;impl nixie_sat::solver::Solver&gt;::minimize_classify (2,314,544,695 sampled cycles, 1.04%)</title><rect x="9.1945%" y="157" width="1.0362%" height="15" fill="rgb(216,153,28)" fg:x="20537430309" fg:w="2314544695"/><text x="9.3820%" y="167.50"></text></g><g><title>nixie_sat::solver::conflict::&lt;impl nixie_sat::solver::Solver&gt;::minimize_literal_plain (3,529,384,804 sampled cycles, 1.58%)</title><rect x="8.6553%" y="173" width="1.5801%" height="15" fill="rgb(241,192,46)" fg:x="19333064563" fg:w="3529384804"/><text x="8.8428%" y="183.50">n..</text></g><g><title>nixie_sat::solver::conflict::&lt;impl nixie_sat::solver::Solver&gt;::shrink_and_minimize_clause (10,431,011,705 sampled cycles, 4.67%)</title><rect x="5.6874%" y="189" width="4.6699%" height="15" fill="rgb(248,99,36)" fg:x="12703737580" fg:w="10431011705"/><text x="5.8749%" y="199.50">nixie_sa..</text></g><g><title>nixie_sat::solver::conflict::AnalysisMark::mark_antecedent (1,874,640,445 sampled cycles, 0.84%)</title><rect x="10.3573%" y="189" width="0.8393%" height="15" fill="rgb(241,94,31)" fg:x="23134749285" fg:w="1874640445"/><text x="10.5448%" y="199.50"></text></g><g><title>nixie_sat::solver::decide::&lt;impl nixie_sat::solver::Solver&gt;::backtrack_with_phase_saving (4,880,309,877 sampled cycles, 2.18%)</title><rect x="11.2669%" y="189" width="2.1849%" height="15" fill="rgb(249,192,32)" fg:x="25166480228" fg:w="4880309877"/><text x="11.4544%" y="199.50">ni..</text></g><g><title>__memset_avx2_unaligned_erms (2,366,865,565 sampled cycles, 1.06%)</title><rect x="14.6474%" y="173" width="1.0596%" height="15" fill="rgb(215,99,14)" fg:x="32717373725" fg:w="2366865565"/><text x="14.8349%" y="183.50"></text></g><g><title>nixie_sat::solver::decide::&lt;impl nixie_sat::solver::Solver&gt;::backtrack (471,282,608 sampled cycles, 0.21%)</title><rect x="15.7446%" y="173" width="0.2110%" height="15" fill="rgb(225,45,0)" fg:x="35168020378" fg:w="471282608"/><text x="15.9321%" y="183.50"></text></g><g><title>nixie_sat::solver::decide::&lt;impl nixie_sat::solver::Solver&gt;::backtrack_with_phase_saving (659,784,401 sampled cycles, 0.30%)</title><rect x="15.9556%" y="173" width="0.2954%" height="15" fill="rgb(249,192,32)" fg:x="35639302986" fg:w="659784401"/><text x="16.1431%" y="183.50"></text></g><g><title>__memset_avx2_unaligned_erms (680,739,888 sampled cycles, 0.30%)</title><rect x="17.7560%" y="109" width="0.3048%" height="15" fill="rgb(215,99,14)" fg:x="39660886785" fg:w="680739888"/><text x="17.9435%" y="119.50"></text></g><g><title>nixie_sat::kitten::Kitten::import_literal (2,105,045,375 sampled cycles, 0.94%)</title><rect x="17.6107%" y="125" width="0.9424%" height="15" fill="rgb(246,67,24)" fg:x="39336227580" fg:w="2105045375"/><text x="17.7982%" y="135.50"></text></g><g><title>alloc::raw_vec::RawVecInner&lt;A&gt;::reserve::do_reserve_and_handle (973,971,224 sampled cycles, 0.44%)</title><rect x="18.1170%" y="109" width="0.4360%" height="15" fill="rgb(233,212,1)" fg:x="40467301731" fg:w="973971224"/><text x="18.3045%" y="119.50"></text></g><g><title>alloc::raw_vec::RawVecInner&lt;A&gt;::finish_grow (921,607,579 sampled cycles, 0.41%)</title><rect x="18.1405%" y="93" width="0.4126%" height="15" fill="rgb(215,110,9)" fg:x="40519665376" fg:w="921607579"/><text x="18.3280%" y="103.50"></text></g><g><title>realloc (649,313,909 sampled cycles, 0.29%)</title><rect x="18.2624%" y="77" width="0.2907%" height="15" fill="rgb(213,61,9)" fg:x="40791959046" fg:w="649313909"/><text x="18.4499%" y="87.50"></text></g><g><title>_int_realloc (596,949,357 sampled cycles, 0.27%)</title><rect x="18.2858%" y="61" width="0.2673%" height="15" fill="rgb(231,75,41)" fg:x="40844323598" fg:w="596949357"/><text x="18.4733%" y="71.50"></text></g><g><title>nixie_sat::kitten::Kitten::clause_with_id_and_exception (3,068,551,949 sampled cycles, 1.37%)</title><rect x="17.2871%" y="141" width="1.3738%" height="15" fill="rgb(242,60,10)" fg:x="38613600767" fg:w="3068551949"/><text x="17.4746%" y="151.50">n..</text></g><g><title>nixie_sat::kitten::Kitten::solve (471,278,142 sampled cycles, 0.21%)</title><rect x="18.6656%" y="141" width="0.2110%" height="15" fill="rgb(252,82,13)" fg:x="41692625620" fg:w="471278142"/><text x="18.8531%" y="151.50"></text></g><g><title>nixie_sat::solver::eliminate::&lt;impl nixie_sat::solver::Solver&gt;::elim_find_definition (4,649,949,386 sampled cycles, 2.08%)</title><rect x="16.8136%" y="157" width="2.0818%" height="15" fill="rgb(238,38,50)" fg:x="37555846734" fg:w="4649949386"/><text x="17.0011%" y="167.50">ni..</text></g><g><title>nixie_sat::solver::eliminate::&lt;impl nixie_sat::solver::Solver&gt;::elim_find_structural_gate (628,373,603 sampled cycles, 0.28%)</title><rect x="18.8953%" y="157" width="0.2813%" height="15" fill="rgb(216,109,28)" fg:x="42205796120" fg:w="628373603"/><text x="19.0828%" y="167.50"></text></g><g><title>nixie_sat::solver::eliminate::&lt;impl nixie_sat::solver::Solver&gt;::elim_resolve_clauses (481,749,649 sampled cycles, 0.22%)</title><rect x="19.5283%" y="141" width="0.2157%" height="15" fill="rgb(246,26,19)" fg:x="43619649555" fg:w="481749649"/><text x="19.7158%" y="151.50"></text></g><g><title>nixie_sat::solver::eliminate::&lt;impl nixie_sat::solver::Solver&gt;::elim_resolvents_bounded (879,716,292 sampled cycles, 0.39%)</title><rect x="19.3595%" y="157" width="0.3938%" height="15" fill="rgb(236,59,38)" fg:x="43242628080" fg:w="879716292"/><text x="19.5470%" y="167.50"></text></g><g><title>nixie_sat::solver::equiv::&lt;impl nixie_sat::solver::Solver&gt;::rebuild_watches_and_binary_graph (816,895,774 sampled cycles, 0.37%)</title><rect x="19.7581%" y="157" width="0.3657%" height="15" fill="rgb(245,85,18)" fg:x="44132817264" fg:w="816895774"/><text x="19.9456%" y="167.50"></text></g><g><title>nixie_sat::solver::eliminate::&lt;impl nixie_sat::solver::Solver&gt;::eliminate_phase (10,682,360,814 sampled cycles, 4.78%)</title><rect x="16.2509%" y="173" width="4.7824%" height="15" fill="rgb(243,214,2)" fg:x="36299087387" fg:w="10682360814"/><text x="16.4384%" y="183.50">nixie_sa..</text></g><g><title>nixie_sat::solver::subsume::&lt;impl nixie_sat::solver::Solver&gt;::subsume_round (2,021,262,752 sampled cycles, 0.90%)</title><rect x="20.1285%" y="157" width="0.9049%" height="15" fill="rgb(223,211,42)" fg:x="44960185449" fg:w="2021262752"/><text x="20.3160%" y="167.50"></text></g><g><title>nixie_sat::preprocessing_core::Preprocessor::pure_literal_elimination (1,131,078,618 sampled cycles, 0.51%)</title><rect x="21.3100%" y="157" width="0.5064%" height="15" fill="rgb(206,8,39)" fg:x="47599356614" fg:w="1131078618"/><text x="21.4975%" y="167.50"></text></g><g><title>alloc::raw_vec::RawVec&lt;T,A&gt;::grow_one (576,023,675 sampled cycles, 0.26%)</title><rect x="21.5585%" y="141" width="0.2579%" height="15" fill="rgb(241,226,9)" fg:x="48154411557" fg:w="576023675"/><text x="21.7460%" y="151.50"></text></g><g><title>alloc::raw_vec::RawVecInner&lt;A&gt;::finish_grow (544,605,562 sampled cycles, 0.24%)</title><rect x="21.5726%" y="125" width="0.2438%" height="15" fill="rgb(215,110,9)" fg:x="48185829670" fg:w="544605562"/><text x="21.7601%" y="135.50"></text></g><g><title>nixie_sat::solver::decide::&lt;impl nixie_sat::solver::Solver&gt;::backtrack (502,682,997 sampled cycles, 0.23%)</title><rect x="22.0415%" y="141" width="0.2250%" height="15" fill="rgb(225,45,0)" fg:x="49233156899" fg:w="502682997"/><text x="22.2290%" y="151.50"></text></g><g><title>nixie_sat::solver::propagate::watch_kernel::Cursor::scan (5,948,600,840 sampled cycles, 2.66%)</title><rect x="23.3027%" y="125" width="2.6632%" height="15" fill="rgb(241,8,32)" fg:x="52050374920" fg:w="5948600840"/><text x="23.4902%" y="135.50">nixi..</text></g><g><title>nixie_sat::solver::propagate::&lt;impl nixie_sat::solver::Solver&gt;::propagate (8,441,170,940 sampled cycles, 3.78%)</title><rect x="22.2665%" y="141" width="3.7791%" height="15" fill="rgb(211,168,5)" fg:x="49735839896" fg:w="8441170940"/><text x="22.4540%" y="151.50">nixie_..</text></g><g><title>nixie_sat::solver::learn::&lt;impl nixie_sat::solver::Solver&gt;::vivify_clauses (9,467,521,159 sampled cycles, 4.24%)</title><rect x="21.8164%" y="157" width="4.2386%" height="15" fill="rgb(206,58,35)" fg:x="48730435232" fg:w="9467521159"/><text x="22.0039%" y="167.50">nixie_s..</text></g><g><title>nixie_sat::solver::subsume::&lt;impl nixie_sat::solver::Solver&gt;::subsume_round (5,089,814,166 sampled cycles, 2.28%)</title><rect x="26.0550%" y="157" width="2.2787%" height="15" fill="rgb(223,211,42)" fg:x="58197956391" fg:w="5089814166"/><text x="26.2425%" y="167.50">nix..</text></g><g><title>nixie_sat::solver::learn::&lt;impl nixie_sat::solver::Solver&gt;::inprocess (16,568,145,420 sampled cycles, 7.42%)</title><rect x="21.0428%" y="173" width="7.4175%" height="15" fill="rgb(234,165,17)" fg:x="47002395352" fg:w="16568145420"/><text x="21.2303%" y="183.50">nixie_sat::sol..</text></g><g><title>nixie_sat::clause::ClauseDatabase::compact_arena_forced (712,157,710 sampled cycles, 0.32%)</title><rect x="28.6994%" y="157" width="0.3188%" height="15" fill="rgb(220,49,49)" fg:x="64104658517" fg:w="712157710"/><text x="28.8869%" y="167.50"></text></g><g><title>nixie_sat::solver::learn::&lt;impl nixie_sat::solver::Solver&gt;::reduce_clause_database (1,434,788,131 sampled cycles, 0.64%)</title><rect x="28.4602%" y="173" width="0.6423%" height="15" fill="rgb(208,133,47)" fg:x="63570540772" fg:w="1434788131"/><text x="28.6477%" y="183.50"></text></g><g><title>nixie_sat::solver::propagate::watch_kernel::Cursor::scan (2,921,966,248 sampled cycles, 1.31%)</title><rect x="29.7027%" y="157" width="1.3082%" height="15" fill="rgb(241,8,32)" fg:x="66345852327" fg:w="2921966248"/><text x="29.8902%" y="167.50"></text></g><g><title>nixie_sat::solver::propagate::&lt;impl nixie_sat::solver::Solver&gt;::propagate (4,178,703,497 sampled cycles, 1.87%)</title><rect x="29.1682%" y="173" width="1.8708%" height="15" fill="rgb(211,168,5)" fg:x="65151951995" fg:w="4178703497"/><text x="29.3557%" y="183.50">ni..</text></g><g><title>nixie_sat::solver::learn::&lt;impl nixie_sat::solver::Solver&gt;::handle_clause_deletion_and_restart (40,016,963,024 sampled cycles, 17.92%)</title><rect x="13.4518%" y="189" width="17.9154%" height="15" fill="rgb(217,187,52)" fg:x="30046790105" fg:w="40016963024"/><text x="13.6393%" y="199.50">nixie_sat::solver::learn::&lt;impl nixie_..</text></g><g><title>nixie_sat::solver::propagate::watch_kernel::Cursor::scan (101,597,684,126 sampled cycles, 45.48%)</title><rect x="51.4066%" y="173" width="45.4848%" height="15" fill="rgb(241,8,32)" fg:x="114824899325" fg:w="101597684126"/><text x="51.5941%" y="183.50">nixie_sat::solver::propagate::watch_kernel::Cursor::scan</text></g><g><title>nixie_sat::solver::propagate::&lt;impl nixie_sat::solver::Solver&gt;::propagate (150,118,541,913 sampled cycles, 67.21%)</title><rect x="31.3813%" y="189" width="67.2074%" height="15" fill="rgb(211,168,5)" fg:x="70095171068" fg:w="150118541913"/><text x="31.5688%" y="199.50">nixie_sat::solver::propagate::&lt;impl nixie_sat::solver::Solver&gt;::propagate</text></g><g><title>nixie_sat::trail::Trail::assign (3,791,129,530 sampled cycles, 1.70%)</title><rect x="96.8914%" y="173" width="1.6973%" height="15" fill="rgb(243,64,53)" fg:x="216422583451" fg:w="3791129530"/><text x="97.0789%" y="183.50">n..</text></g><g><title>nixie_sat::vsids::VSIDS::bump_batch (2,597,300,259 sampled cycles, 1.16%)</title><rect x="98.7622%" y="189" width="1.1628%" height="15" fill="rgb(231,124,54)" fg:x="220601209794" fg:w="2597300259"/><text x="98.9497%" y="199.50"></text></g><g><title>nixie_sat::vsids::VSIDS::sift_up (1,874,679,337 sampled cycles, 0.84%)</title><rect x="99.0857%" y="173" width="0.8393%" height="15" fill="rgb(244,24,51)" fg:x="221323830716" fg:w="1874679337"/><text x="99.2732%" y="183.50"></text></g><g><title>nixie_sat::solver::search_ext::&lt;impl nixie_sat::solver::Solver&gt;::solve_with_theory (223,292,764,602 sampled cycles, 99.97%)</title><rect x="0.0281%" y="205" width="99.9672%" height="15" fill="rgb(240,225,15)" fg:x="62841153" fg:w="223292764602"/><text x="0.2156%" y="215.50">nixie_sat::solver::search_ext::&lt;impl nixie_sat::solver::Solver&gt;::solve_with_theory</text></g><g><title>all (223,366,076,481 sampled cycles, 100%)</title><rect x="0.0000%" y="333" width="100.0000%" height="15" fill="rgb(206,183,37)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="343.50"></text></g><g><title>Captured caller chain (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="317" width="100.0000%" height="15" fill="rgb(235,229,51)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="327.50">Captured caller chain</text></g><g><title>_start (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="301" width="100.0000%" height="15" fill="rgb(210,219,27)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="311.50">_start</text></g><g><title>__libc_start_main@@GLIBC_2.34 (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="285" width="100.0000%" height="15" fill="rgb(217,176,50)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="295.50">__libc_start_main@@GLIBC_2.34</text></g><g><title>__libc_start_call_main (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="269" width="100.0000%" height="15" fill="rgb(249,173,1)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="279.50">__libc_start_call_main</text></g><g><title>main (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="253" width="100.0000%" height="15" fill="rgb(238,28,47)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="263.50">main</text></g><g><title>std::sys::backtrace::__rust_begin_short_backtrace (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="237" width="100.0000%" height="15" fill="rgb(208,179,44)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="247.50">std::sys::backtrace::__rust_begin_short_backtrace</text></g><g><title>stats_solve::main (223,366,076,481 sampled cycles, 100.00%)</title><rect x="0.0000%" y="221" width="100.0000%" height="15" fill="rgb(227,19,29)" fg:x="0" fg:w="223366076481"/><text x="0.1875%" y="231.50">stats_solve::main</text></g></svg></svg>